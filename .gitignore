# ── Secrets — NEVER commit ──────────────────────────────────────────────
Credentials.env
.env
.pusher.env
.streamlit/secrets.toml
.streamlit/machine_id*

# ── Snapshot artifact (lives on the `snapshot` branch, not main) ─────────
snapshot.json

# ── Pusher runtime log ───────────────────────────────────────────────────
pusher.log

# ── Research-Ballot: separate repo (apex-platform), cloned in for local work ─
Research-Ballot/

# ── Tunnel binary (standalone download, not source) ──────────────────────
cloudflared.exe

# ── MT5 live-quote bridge: local secrets + tunnel creds (NEVER commit) ────
mt5_service/service.env
mt5_service/tunnel.yml
mt5_service/*.json
.cloudflared/

# ── Ready-to-paste Streamlit Cloud secrets (contains a token) ────────────
streamlit_secrets_to_paste.toml

# ── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/

# ── OS / editor ─────────────────────────────────────────────────────────
Thumbs.db
.DS_Store
.vscode/
.idea/
